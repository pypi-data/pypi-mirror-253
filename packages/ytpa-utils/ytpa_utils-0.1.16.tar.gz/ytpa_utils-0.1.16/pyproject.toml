[tool.poetry]
name = "ytpa-utils"
version = "0.1.16"
description = "Utils for the YouTube Predictive Analytics system"
authors = ["Johannes Traa"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11.5"
pandas = "^2.0.3"
numpy = "^1.25.2"

[tool.poetry.dev-dependencies]
pytest = "^7.4.3"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"