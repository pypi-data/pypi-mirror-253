


#
#	How to publish:
#
#		(rm -rf dist && python3 -m build --sdist && twine upload dist/*)
#		git push --set-upstream git@gitlab.com:reptilian_climates/modules_series_3/proceeds.git climate
#
#		git@gitlab.com:modules_series_3/proceeds.git
#

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "proceeds"
version = "0.0.5"

license = { text = "GPL 3.0 + OFL" }

description = "resume creator"



readme = "module.txt"

[tool.setuptools.packages.find]
where = [ "fields/structures" ]

#
#	https://setuptools.pypa.io/en/latest/userguide/datafiles.html
#
[tool.setuptools.package-data]
"*" = [ "*.html" ]