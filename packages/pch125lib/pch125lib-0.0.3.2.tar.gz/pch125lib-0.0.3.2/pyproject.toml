[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "pch125lib"
description = "My library is about defs of my collection"
version = "0.0.3.2"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
            "numpy",
            "roman"
            ]