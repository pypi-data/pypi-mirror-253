[tool.poetry]
name = "is-propchecker"
version = "0.1.0"
description = ""
authors = ["neoskye24 <neoskyeai@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
is-propchecker = "is_propchecker.main:app"

[tool.poetry.dependencies]
python = "^3.10"
typer = {extras = ["all"], version = "^0.9.0"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
