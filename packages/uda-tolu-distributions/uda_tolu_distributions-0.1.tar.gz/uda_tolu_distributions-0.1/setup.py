from setuptools import setup

setup(name='uda_tolu_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['uda_tolu_distributions'],
      zip_safe=False)
