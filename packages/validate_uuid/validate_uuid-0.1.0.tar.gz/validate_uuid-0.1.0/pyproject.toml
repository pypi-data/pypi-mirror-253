[tool.poetry]
name = "validate_uuid"
version = "0.1.0"
description = "Проверка УИд на валидность"
authors = ["t1bon <tim.batrudinov@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"