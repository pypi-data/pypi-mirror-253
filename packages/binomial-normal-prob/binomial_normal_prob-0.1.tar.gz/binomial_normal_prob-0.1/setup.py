from setuptools import setup

setup(name='binomial_normal_prob',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['binomial_normal_prob'],
      zip_safe=False)