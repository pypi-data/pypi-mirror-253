[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ops-py-cert-report"
description = "Output a Cert expire report"
license = {file = ""}
version = "1.0.0"
dynamic = ["readme", "dependencies"]

[tool.setuptools.dynamic]
readme = {file = "", content-type = "text/markdown"}
dependencies = {file = [""]}
