[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "antarctic_plots"
authors = [
    {name = "Matt Tankersley", email = "matt.d.tankersley@gmail.com"},
]
description = "antarctic_plots is now polartoolkit"
requires-python = ">=3.9"

readme = "README.md"
version = "0.1.2"
license = {file = "LICENSE"}

dependencies = [
    "polartoolkit"
]
