test.service:
  files:
    /tmp/file1:
      disabled:
        type: jinja
        jinja: '{% if rougail.general.condition == "yes" or rougail.general.condition
          == "maybe" %}false{% else %}true{% endif %}'
version: '1.0'
