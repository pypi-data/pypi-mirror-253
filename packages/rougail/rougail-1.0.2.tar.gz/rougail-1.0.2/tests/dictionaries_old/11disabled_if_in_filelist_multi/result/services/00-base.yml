test.service:
  files:
    /tmp/file1:
      disabled:
        type: jinja
        jinja: '{% if rougail.general.condition == "oui" %}true{% else %}false{% endif
          %}'
    /tmp/file2:
      disabled:
        type: jinja
        jinja: '{% if rougail.general.condition == "oui" %}true{% else %}false{% endif
          %}'
version: '1.0'
