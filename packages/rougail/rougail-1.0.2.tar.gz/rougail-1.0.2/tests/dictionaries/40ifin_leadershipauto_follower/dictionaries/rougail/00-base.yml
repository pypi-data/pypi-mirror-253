version: '1.0'
general:
  mode_conteneur_actif:
    type: string
    description: No change
    default: non
  condition:
    type: string
    description: condition
    default: oui
  leader:
    description: leader
    type: leadership
    leader:
      type: string
      description: leader
      multi: true
      default:
      - a
      - b
    follower1:
      type: string
      description: follower1
      disabled:
        type: jinja
        jinja: '{% if rougail.general.leader.leader == "a" %}

          the value of "leader" is "a"

          {% endif %}

          '
      default:
        type: jinja
        jinja: '{{ calc_val() }}'
      mandatory: false
    follower2:
      type: string
      description: follower2
      mandatory: false
