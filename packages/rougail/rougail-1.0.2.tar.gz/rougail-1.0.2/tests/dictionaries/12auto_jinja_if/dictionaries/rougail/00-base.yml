general:
  mode_conteneur_actif:
    type: string
    description: No change
    hidden: true
    default:
      type: jinja
      jinja: '{% if rougail.general.mode_conteneur_actif1 == ''non'' %}non{% else
        %}oui{% endif %}'
    mandatory: false
  mode_conteneur_actif1:
    type: string
    description: No change
    default: non
  mode_conteneur_actif2:
    type: string
    description: No change
    hidden: true
    default:
      type: jinja
      jinja: '{% if rougail.general.mode_conteneur_actif1 == "non" %}oui{% else %}non{%
        endif %}'
    mandatory: false
version: '1.0'
