from setuptools import find_packages, setup

setup(
    name='stone_color',
    packages=find_packages(),
    version='0.1.3',
    description='Text formatting, coloring and tables library',
    keywords=["color", "cli", "table", "terminal-colors", "ansi", "rgb"],
    author='aniko33',
)
