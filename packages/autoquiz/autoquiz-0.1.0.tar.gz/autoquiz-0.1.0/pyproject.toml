[tool.poetry]
name = "autoquiz"
version = "0.1.0"
description = "Create test from study notes"
authors = ["jose <jose.s.contacto@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
click = "^8.1.7"

[tool.poetry.group.dev.dependencies]
pytest-watch = "^4.2.0"
behave = "^1.2.6"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
