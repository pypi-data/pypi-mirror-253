[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "PCH_LIB"
description = "My Collection"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
            "numpy",
            "roman"
            ]