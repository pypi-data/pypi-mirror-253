from setuptools import setup,find_packages

setup(
    name="trial-initRelated",
    description='a basic code to host a simple flask app',
    packages=find_packages(),
    version='0.1.0'
)
