"""Module providing a function for inventory navigation."""
import pyautogui

def mouse_to_inventory_slot_1_1(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('58' + a + '.'+ b + c)
    y = float('255.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_1_2(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('63' + a + '.'+ b + c)
    y = float('255.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_1_3(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('67' + a + '.'+ b + c)
    y = float('255.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_1_4(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('70' + a + '.'+ b + c)
    y = float('255.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_2_1(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('58' + a + '.'+ b + c)
    y = float('285.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_2_2(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('63' + a + '.'+ b + c)
    y = float('285.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_2_3(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('67' + a + '.'+ b + c)
    y = float('285.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_2_4(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('70' + a + '.'+ b + c)
    y = float('285.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_3_1(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('58' + a + '.'+ b + c)
    y = float('325.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_3_2(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('63' + a + '.'+ b + c)
    y = float('325.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_3_3(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('67' + a + '.'+ b + c)
    y = float('325.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_3_4(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('70' + a + '.'+ b + c)
    y = float('325.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_4_1(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('58' + a + '.'+ b + c)
    y = float('354.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_4_2(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('63' + a + '.'+ b + c)
    y = float('354.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_4_3(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('67' + a + '.'+ b + c)
    y = float('354.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_4_4(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('70' + a + '.'+ b + c)
    y = float('354.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_5_1(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('58' + a + '.'+ b + c)
    y = float('390.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_5_2(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('63' + a + '.'+ b + c)
    y = float('390.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_5_3(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('67' + a + '.'+ b + c)
    y = float('390.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_5_4(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('70' + a + '.'+ b + c)
    y = float('390.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_6_1(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('58' + a + '.'+ b + c)
    y = float('440.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_6_2(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('63' + a + '.'+ b + c)
    y = float('440.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_6_3(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('67' + a + '.'+ b + c)
    y = float('440.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_6_4(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('70' + a + '.'+ b + c)
    y = float('440.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_7_1(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('58' + a + '.'+ b + c)
    y = float('475.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_7_2(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('63' + a + '.'+ b + c)
    y = float('475.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_7_3(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('67' + a + '.'+ b + c)
    y = float('475.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)

def mouse_to_inventory_slot_7_4(a, b, c):
    """Moves mouse to inventory slot"""
    x = float('70' + a + '.'+ b + c)
    y = float('475.'+ b + c)
    wait_time = float('.17' + b + a + c)
    pyautogui.moveTo(x, y, wait_time)
