# the init file contains the executed code when a library is called,
# on this case when you execute the "from hack4u import *"
from .courses import *
#iport all the funcations of the module courses by default
from .utils import *
#makes the same with the utils file
