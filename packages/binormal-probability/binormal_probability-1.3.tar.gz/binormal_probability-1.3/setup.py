from setuptools import setup

setup(name='binormal_probability',
      version='1.3',
      description='Gaussian and Binomial distributions',
      packages=['binormal_probability'],
      zip_safe=False)
