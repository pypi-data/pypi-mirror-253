from setuptools import setup, find_packages

setup(
    name='datasendnetwork',
    version='1.0.0',
    description='tranferencias de datos ',
    packages=find_packages(),
    install_requires=[
        'requests',  # y otras dependencias necesarias
    ],
)
