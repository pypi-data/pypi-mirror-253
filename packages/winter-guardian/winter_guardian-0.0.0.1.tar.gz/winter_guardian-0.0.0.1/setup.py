from setuptools import setup, find_packages

setup(
    name="winter_guardian",
    version="0.0.0.1",
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
        # e.g., 'requests>=2.25.1',
    ],
    # More metadata like author, description, etc.
)
